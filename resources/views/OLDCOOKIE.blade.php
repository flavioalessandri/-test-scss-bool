<?php

// $cookie = $request->cookie();
//
// Cookie::queue($apart->id, $user_ip, 2);
//
//
// dd($request->cookie());
// dd($value);
// response()->with($cookie);
// return response('Hello World')->cookie(
// 'name', 'value', $minutes, $path, $domain, $secure, $httpOnly
// ) ;
//
// // $cookie= Cookie::queue($apart->id, $user_ip, $minutes);
// //
// // dd(cookie('name'));
// $mycookie= $request->cookie($user_ip, $apart->id, $minutes);
// $mycookie= $request->cookie($user_ip, $apart->id, $minutes);
//
// $value = Cookie::get('name');

// dd($value);


//
// $minutes=2;

// $mycookie= Cookie::queue($user_ip, 'value', $minutes);

// $response = new Illuminate\Http\Response('Hello World');

  // $response->withCookie($user_ip, 'value', $minutes);



// $cookie = Cookie::forget('name');

// $cookie= Cookie::queue('userip', $user_ip, 1);
//
// dd($cookie);

// return response()->withCookie($cookie);



      // Cookie::queue(Cookie::make('seen_posts', json_encode([1,2]), $minutes));
      //
      // $seen_posts = json_decode($request->cookie('seen_posts'),true);
      //
      // $seen_posts[] = 3;
      //
      // Cookie::queue(Cookie::make('seen_posts', json_encode($seen_posts), $minutes));


      // $cookieValue = $request->cookie('userip');

      // if ($cookieValue == $user_ip) {
      //
      //   var_dump($cookieValue);
      //
      // } else {
      //
      //   dd($stat);
      // }
